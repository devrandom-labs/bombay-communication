[package]
name = "fastpass-testkit"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

# The shared property suite (P1–P7). Both `fastpass` (kimi's target) and
# `fastpass-reference` (the gold impl) instantiate the SAME test bodies against
# their own `channel()` via the exported macros — one oracle, two subjects.
[lib]
path = "src/lib.rs"

[dependencies]

[lints]
workspace = true
